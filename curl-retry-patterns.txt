# Domains that frequently drop connections when hit by lychee in CI but
# respond fine to a single curl request. URLs matching these patterns are
# retried with curl before being included in the error report.
#
# One grep -E pattern per line. Blank lines and # comments are ignored.

cert-manager\.io
envoyproxy\.io
gateway-api\.sigs\.k8s\.io
git\.k8s\.io
istio\.io
kubernetes\.io
opentelemetry\.io
raw\.githubusercontent\.com
